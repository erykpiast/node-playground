{
  "name": "anti-cross-domain-test",
  "version": "0.0.1",
  "description": "test for blocking Cross-Domain requests",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "compression": "^1.4.3",
    "express": "^4.10.1"
  }
}
